# Copy to .env and fill in. .env is gitignored — secrets never live in the repo.
DATABASE_URL=postgresql+psycopg://ttcloud:devonly@localhost:5460/ttcloud
POSTGRES_PASSWORD=devonly
SECRET_KEY=change-me-in-production
